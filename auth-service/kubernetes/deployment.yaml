apiVersion: apps/v1
kind: Deployment
metadata:
  name: auth-service-deployment
  namespace: event
  labels:
    app.kubernetes.io/name: auth-service
    app.kubernetes.io/part-of: event
spec:
  replicas: 2
  selector:
    matchLabels:
      app.kubernetes.io/name: auth-service
  template:
    metadata:
      labels:
        app.kubernetes.io/name: auth-service
    spec:
      securityContext:
        runAsNonRoot: true
        seccompProfile:
          type: RuntimeDefault
      containers:
      - name: auth-service
        image: 235704034905.dkr.ecr.ap-northeast-2.amazonaws.com/auth-service:0.0.1
        imagePullPolicy: Always
        ports:
        - name: http
          containerPort: 8080
        securityContext:
          runAsUser: 1000
          allowPrivilegeEscalation: false
        envFrom:
        - configMapRef:
            name: auth-service-config
        - secretRef:
            name: auth-service-secret
        resources:
          requests:
            cpu: 250m
            memory: 512Mi
          limits:
            cpu: "1"
            memory: "1Gi"
        readinessProbe:
          httpGet:
            path: /auth/actuator/health/readiness
            port: 8080
          initialDelaySeconds: 20
          failureThreshold: 3
          periodSeconds: 5
          timeoutSeconds: 3
        livenessProbe:
          httpGet:
            path: /auth/actuator/health/liveness
            port: 8080
          initialDelaySeconds: 60
          failureThreshold: 3
          periodSeconds: 10
          timeoutSeconds: 3
  strategy:
    type: RollingUpdate
    rollingUpdate:
      maxSurge: 1
      maxUnavailable: 0